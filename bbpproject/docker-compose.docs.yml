services:
  docs:
    image: bbpproject_local_docs
    container_name: bbpproject_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - /app/.venv
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./bbpproject:/app/bbpproject:z
    ports:
      - '9000:9000'
    command: /start-docs
